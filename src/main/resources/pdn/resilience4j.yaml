resilience4j.retry:
  instances:
    feignClient:
      maxAttempts: 4
      waitDuration: 1s
      enableExponentialBackoff: true
      exponentialBackoffMultiplier: 2
        #retryExceptions:
      #- feign.RetryableException